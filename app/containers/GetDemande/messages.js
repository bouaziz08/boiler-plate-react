/*
 * GetDemande Messages
 *
 * This contains all the text for the GetDemande container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GetDemande';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GetDemande container!',
  },
});
