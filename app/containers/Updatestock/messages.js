/*
 * Updatestock Messages
 *
 * This contains all the text for the Updatestock container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Updatestock';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Updatestock container!',
  },
});
