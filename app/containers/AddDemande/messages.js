/*
 * AddDemande Messages
 *
 * This contains all the text for the AddDemande container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddDemande';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddDemande container!',
  },
});
